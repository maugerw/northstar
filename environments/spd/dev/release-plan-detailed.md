# Release Plan (Detailed, Step-by-Step) — SOA Suite Domain JMS / Infrastructure Rebuild

**Domain / Environment:** `spd` / `dev`
**Audience:** this version assumes no familiarity with this specific migration.
Every object is written out individually — there is no "repeat this procedure
for each row" step. Work top to bottom, tick each box, do not skip ahead.

**Source of values:** `export.json` (generated by `extract_objects.py`), domain
`spd` / environment `dev`.
**Target:** WebLogic Admin Console (manual, click-through deployment).
**Companion file:** `release-plan.md` in this same folder is the condensed
version of this plan (procedures + lookup tables).
**Generated by:** `gorelease` (Go port of `extract/gen_release_plan.py`) — re-run against a fresh export if
the source domain's config changes; do not hand-edit this file, edit the export
or the generator instead.

---

## 0. Before You Start

- [ ] You have the Admin Console URL and an admin login for the **target**
      domain.
- [ ] You know whether the target domain's server/cluster names match the
      source domain's names exactly. The source names used throughout this
      plan are: `AdminServer`, `osb_cluster`, `osb_server1`, `soa_cluster`, `soa_server1`. **If your target uses different names, substitute your
      names everywhere this plan says one of those names — object names
      (data sources, stores, queues, etc.) are NOT renamed, only these
      infrastructure names.**
- [ ] You have the 15 database passwords for the data sources in Phase 1
      (from vault / DBA — none were exported from the source domain).
- [ ] The 2 adapter `.rar` files and 2 deployment plan `.xml` files (Phase 6)
      are staged somewhere the target Admin Server can read them.
- [ ] You've taken a config backup / snapshot of the target domain.
- [ ] **This plan is out of scope for:** SOA composites, OSB projects, and
      any system/internal WebLogic objects that come from the domain
      template or product install.

---

## 1. Starting an Edit Session

Do this once now, and again at the start of every Phase below (a Phase always
ends with **Activate Changes**, which closes the session).

1. [ ] Open a browser to the target Admin Console, e.g.
       `http://<admin-host>:<port>/console`.
2. [ ] Log in with the admin account.
3. [ ] Top-left corner: find the **Change Center** panel.
4. [ ] Click **Lock & Edit**. The console now shows you are in an edit
       session — everything you do below is staged, not live, until you
       activate it.

**At the end of each Phase** (marked with a ➡ in this document):

5. [ ] Click **Activate Changes** in the Change Center.
6. [ ] If the console reports errors for that phase, click **Undo All
       Changes**, fix the problem, and start that phase's steps again from
       Lock & Edit.
7. [ ] If it succeeds, move to the next Phase and start a fresh Lock & Edit.

Do not activate mid-phase — a partially-created phase can leave dangling
references.

---

## 2. Order of Work

Do the phases strictly in this order. Each later phase references objects
created in an earlier one.

1. Phase 1 — 15 JDBC Data Sources
2. Phase 2 — 1 Persistent Stores (1 File Stores + 0 JDBC Stores)
3. Phase 3 — 1 JMS Servers
4. Phase 4 — 1 SAF Agents
5. Phase 5 — 2 JMS Modules (with their subdeployments, queues, connection
   factories, and SAF error handlings)
6. Phase 6 — 2 Adapter Deployments

---

# PHASE 1 — JDBC Data Sources

Start a fresh **Lock & Edit** session (§1) before object 1.1 below.

For every data source in this phase you will do the same six-page wizard,
then a post-creation step to overwrite three fields the wizard doesn't set
correctly (URL, Driver Class Name, Properties) plus the password and pool
sizing. This is deliberate — the wizard's assembled values are not trusted;
what you type in the **Post-Step** for each object below is the value that
must end up saved.

---

## 1.1 Data Source: `FaultDBTest`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `FaultDBTest`
   - JNDI Name: `jdbc/FaultDBTest`
   - Database Type: ***(unrecognised driver -- pick manually)***
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `weblogic.jdbcx.sqlserver.SQLServerDataSource`
       — **weblogic.jdbcx.sqlserver.SQLServerDataSource**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `Faults_UAT`
   - Host Name: `lstnrOMSDEV`
   - Port: `1433`
   - Database User Name: `genfaultdbuser`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:weblogic:sqlserver://lstnrOMSDEV:1433;allowPortWithNamedInstance=true`
   - Driver Class Name: `weblogic.jdbcx.sqlserver.SQLServerDataSource`
   - Properties: `databaseName=Faults_UAT`, `portNumber=1433`, `serverName=lstnrOMSDEV`, `user=genfaultdbuser`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL SELECT 1`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.2 Data Source: `FaultDatabase`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `FaultDatabase`
   - JNDI Name: `jdbc/FaultDatabase`
   - Database Type: **MS SQL Server**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `weblogic.jdbc.sqlserver.SQLServerDriver`
       — **Microsoft's MS SQL Server Driver (Type 4 XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, but set the radio to **One-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `Faults_DEV`
   - Host Name: `lstnromsdev.uk.ssegroup.net`
   - Port: `1433`
   - Database User Name: `genfaultdbuser`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:weblogic:sqlserver://lstnromsdev.uk.ssegroup.net:1433;allowPortWithNamedInstance=true`
   - Driver Class Name: `weblogic.jdbc.sqlserver.SQLServerDriver`
   - Properties: `databaseName=Faults_DEV`, `portNumber=1433`, `serverName=lstnromsdev.uk.ssegroup.net`, `user=genfaultdbuser`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL SELECT 1`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.3 Data Source: `FaultDatabaseXA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `FaultDatabaseXA`
   - JNDI Name: `jdbc/FaultDatabaseXA`
   - Database Type: ***(unrecognised driver -- pick manually)***
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `weblogic.jdbcx.sqlserver.SQLServerDataSource`
       — **weblogic.jdbcx.sqlserver.SQLServerDataSource**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `Faults_DEV`
   - Host Name: `lstnromsdev.uk.ssegroup.net`
   - Port: `1433`
   - Database User Name: `genfaultdbuser`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:weblogic:sqlserver://lstnromsdev.uk.ssegroup.net:1433;allowPortWithNamedInstance=true`
   - Driver Class Name: `weblogic.jdbcx.sqlserver.SQLServerDataSource`
   - Properties: `databaseName=Faults_DEV`, `portNumber=1433`, `serverName=lstnromsdev.uk.ssegroup.net`, `user=genfaultdbuser`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL SELECT 1`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.4 Data Source: `MPRS_DMT_NORTH_CGI_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_DMT_NORTH_CGI_XA`
   - JNDI Name: `jdbc/MPRS_DMT_NORTH_CGI_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `mprsdmtn`
   - Host Name: `clsuc010-scan.uk.ssegroup.net`
   - Port: `1602`
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//clsuc010-scan.uk.ssegroup.net:1602/mprsdmtn`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.5 Data Source: `MPRS_DMT_SOUTH_CGI_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_DMT_SOUTH_CGI_XA`
   - JNDI Name: `jdbc/MPRS_DMT_SOUTH_CGI_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `mprsdmts`
   - Host Name: `clsuc010-scan.uk.ssegroup.net`
   - Port: `1602`
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//clsuc010-scan.uk.ssegroup.net:1602/mprsdmts`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.6 Data Source: `MPRS_NORTH_CGI_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_NORTH_CGI_XA`
   - JNDI Name: `jdbc/MPRS_NORTH_CGI_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `mprsdmtn`
   - Host Name: `clsuc010-scan.uk.ssegroup.net`
   - Port: `1602`
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//clsuc010-scan.uk.ssegroup.net:1602/mprsdmtn`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.7 Data Source: `MPRS_SOA_NORTH_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_SOA_NORTH_XA`
   - JNDI Name: `jdbc/MPRS_SOA_NORTH_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.OracleDriver`
       — **Oracle's Driver (Thin) for Service connections (non-XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `osb_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS_LIST=(ADDRESS=(PROTOCOL=TCP)(HOST=clsuc010-scan.uk.ssegroup.net)(PORT=1602)))(CONNECT_DATA=(SERVICE_NAME=mprsdevn)))`
   - Driver Class Name: `oracle.jdbc.OracleDriver`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.8 Data Source: `MPRS_SOA_SOUTH_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_SOA_SOUTH_XA`
   - JNDI Name: `jdbc/MPRS_SOA_SOUTH_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.OracleDriver`
       — **Oracle's Driver (Thin) for Service connections (non-XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `osb_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS_LIST=(ADDRESS=(PROTOCOL=TCP)(HOST=clsuc010-scan.uk.ssegroup.net)(PORT=1602)))(CONNECT_DATA=(SERVICE_NAME=mprsdevs)))`
   - Driver Class Name: `oracle.jdbc.OracleDriver`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.9 Data Source: `MPRS_SOUTH_CGI_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `MPRS_SOUTH_CGI_XA`
   - JNDI Name: `jdbc/MPRS_SOUTH_CGI_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `mprsdmts`
   - Host Name: `clsuc010-scan.uk.ssegroup.net`
   - Port: `1602`
   - Database User Name: `MPRS_SOA`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//clsuc010-scan.uk.ssegroup.net:1602/mprsdmts`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=MPRS_SOA`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.10 Data Source: `SIMSIntegration_NonXA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SIMSIntegration_NonXA`
   - JNDI Name: `jdbc/SIMSIntegration_NonXA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.OracleDriver`
       — **Oracle's Driver (Thin) for Service connections (non-XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, but set the radio to **One-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `SIMSTST`
   - Host Name: `havud301`
   - Port: `1526`
   - Database User Name: `sims2`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `osb_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//havud301:1526/SIMSTST`
   - Driver Class Name: `oracle.jdbc.OracleDriver`
   - Properties: `user=sims2`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.11 Data Source: `SIMSOMSIntegration`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SIMSOMSIntegration`
   - JNDI Name: `jdbc/SIMSOMSIntegration`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.OracleDriver`
       — **Oracle's Driver (Thin) for Service connections (non-XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, but set the radio to **One-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `SIMSTST`
   - Host Name: `havud301`
   - Port: `1526`
   - Database User Name: `sims2`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havud301:1526:SIMSTST`
   - Driver Class Name: `oracle.jdbc.OracleDriver`
   - Properties: `user=sims2`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.12 Data Source: `SIMSOMSIntegration_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SIMSOMSIntegration_XA`
   - JNDI Name: `jdbc/SIMSOMSIntegration_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `SIMSTST`
   - Host Name: `havud301`
   - Port: `1526`
   - Database User Name: `sims2`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havud301:1526:SIMSTST`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=sims2`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `20`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.13 Data Source: `SIMS_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SIMS_XA`
   - JNDI Name: `jdbc/SIMS_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `simsdev`
   - Host Name: `havud301`
   - Port: `1526`
   - Database User Name: `smart_interface`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `osb_server1`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havud301:1526:simsdev`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=smart_interface`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.14 Data Source: `SSE_XXCUST_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SSE_XXCUST_XA`
   - JNDI Name: `jdbc/SSE_XXCUST_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `DBMRDV01`
   - Host Name: `havut049`
   - Port: `1526`
   - Database User Name: `SSE_XXCUST`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havut049:1526:DBMRDV01`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `autocommit=true`, `user=SSE_XXCUST`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `3`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.15 Data Source: `TestXA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `TestXA`
   - JNDI Name: `jdbc/TestXA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `DBMRDV01`
   - Host Name: `havut049`
   - Port: `1526`
   - Database User Name: `SSE_XXCUST`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havut049:1526:DBMRDV01`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=SSE_XXCUST`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

---

## ➡ End of Phase 1

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Data Sources** lists all 15: `FaultDBTest`, `FaultDatabase`, `FaultDatabaseXA`, `MPRS_DMT_NORTH_CGI_XA`, `MPRS_DMT_SOUTH_CGI_XA`, `MPRS_NORTH_CGI_XA`, `MPRS_SOA_NORTH_XA`, `MPRS_SOA_SOUTH_XA`, `MPRS_SOUTH_CGI_XA`, `SIMSIntegration_NonXA`, `SIMSOMSIntegration`, `SIMSOMSIntegration_XA`, `SIMS_XA`, `SSE_XXCUST_XA`, `TestXA`.
- [ ] For each of the 15 **targeted** ones (`FaultDBTest`, `FaultDatabase`, `FaultDatabaseXA`, `MPRS_DMT_NORTH_CGI_XA`, `MPRS_DMT_SOUTH_CGI_XA`, `MPRS_NORTH_CGI_XA`, `MPRS_SOA_NORTH_XA`, `MPRS_SOA_SOUTH_XA`, `MPRS_SOUTH_CGI_XA`, `SIMSIntegration_NonXA`, `SIMSOMSIntegration`, `SIMSOMSIntegration_XA`, `SIMS_XA`, `SSE_XXCUST_XA`, `TestXA`), open it → **Monitoring → Testing** tab → select a target server →
      click **Test Data Source**. Confirm success (this requires the
      password to be correct).

---

# PHASE 2 — Persistent Stores

Start a fresh **Lock & Edit** session before object 2.1.

There are two kinds: **File Stores** (data written to disk files) and
**JDBC Stores** (data written to a database table via a data source). Do
all 1 file stores first, then all 0 JDBC stores — order between them
doesn't matter, but both must finish before Phase 3.

---

## 2.1 File Store: `GIS_SOA_FileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `GIS_SOA_FileStore`. Target: `soa_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

---

## ➡ End of Phase 2

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Persistent Stores** lists all 1: the 1 file
      stores (`GIS_SOA_FileStore`) and the 0 JDBC stores (), each with the target you set
      above.

---

# PHASE 3 — JMS Servers

Start a fresh **Lock & Edit** session before object 3.1. Each JMS server
below references a persistent store created in Phase 2 — Phase 2 must be
activated first.

## 3.1 JMS Server: `SMIJMSServer1`

1. [ ] Domain Structure → **Services → Messaging → JMS Servers → New**.
2. [ ] Name: `SMIJMSServer1`.
3. [ ] Persistent Store: `FileStore_auto_1`. Click **Next**.
4. [ ] Target: `osb_server1 (migratable)`. Click **Finish**.
5. [ ] Leave Bytes Maximum / Messages Maximum at their unlimited defaults —
       no change needed.

---

## ➡ End of Phase 3

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Messaging → JMS Servers** lists all 1: `SMIJMSServer1` — each
      bound to the persistent store you set.
- [ ] Check **Monitoring** tab for each — Health should read OK once the
      target server has restarted (or immediately if this is a dynamic
      cluster that doesn't require a restart).

---

# PHASE 4 — SAF (Store-and-Forward) Agents

Start a fresh **Lock & Edit** session before object 4.1.

**Every agent below shares these common advanced settings — set them on
each agent after creating it, exactly as listed:**
- Retry Delay Base: `20000` ms
- Retry Delay Maximum: `180000` ms
- Retry Multiplier: `1`
- Acknowledge Interval: `-1` (this is the console default — leave it alone)
- Window Size: `10`
- Time-To-Live: `0` (unlimited)
- **Logging: ENABLED** ⚠️ this is not the console default (default is disabled) — every one of the agents below must have logging turned ON.

## 4.1 SAF Agent: `GIS_SAF_SOA_Agent`

1. [ ] Domain Structure → **Services → Messaging → Store-and-Forward Agents
       → New**.
2. [ ] Name: `GIS_SAF_SOA_Agent`.
3. [ ] Persistent Store: `GIS_SOA_FileStore`.
4. [ ] Agent Type: **Sending-only** (this agent only sends (forwards outbound messages)). Click **Next**.
5. [ ] Target: `soa_server1`. Click **Finish**.
6. [ ] Open the agent → set the common settings listed above, including
       **Logging = ENABLED**. Save.

---

## ➡ End of Phase 4

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Messaging → Store-and-Forward Agents** lists all 1:
      `GIS_SAF_SOA_Agent`.
- [ ] Open each and confirm Logging matches the value noted above — this is
      easy to miss since it isn't always the console default.

---

# PHASE 5 — JMS Modules

Start a fresh **Lock & Edit** session before object 5.1. Each module below
is done completely (module → subdeployment(s) → destinations/CFs/error
handlings) before moving to the next module.

**General pattern for every module in this phase — the exact wizard steps
you'll repeat, spelled out once here for reference. Each module section
below tells you exactly what values to type at each of these steps; you
don't need to look anything up elsewhere.**

- *Create the module itself:* **Services → Messaging → JMS Modules → New**
  → type Name → Next → tick Targets → Next → Finish.
- *Add a subdeployment* (do this before adding any destination that uses
  it): open the module → **Subdeployments** tab → New → type Subdeployment
  Name → Next → tick the Targets (a JMS server, a SAF agent, or a cluster,
  depending on the module) → Finish.
- *Add a Queue / Topic:* open the module → New → select the destination
  type → Next → type Name and JNDI Name → Next → select the Subdeployment
  → Finish.
- *Add a Distributed Queue/Topic (Uniform or plain):* same as above but
  select the distributed variant → after Finish, open it and set Load
  Balancing Policy.
- *Add a Connection Factory (default-targeted):* open the module → New →
  **Connection Factory** → Next → type Name and JNDI Name → Next → on the
  Targeting page, **leave "Default Targeting Enabled" checked and do not
  select a Subdeployment** → Finish.
- *Add a SAF Error Handling:* open the module → New → **SAF Error Handling**
  → Next → type Name → select Policy → Finish.

---

## 5.1 Module: `GIS_SAF_SOA_JMS_Module`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `GIS_SAF_SOA_JMS_Module`. Click **Next**.
3. [ ] Targets: `soa_server1`. Click **Next**, then **Finish**.

**Add subdeployment `GIS_SAF_SOA_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `GIS_SAF_SOA_Subdeployment`. Click **Next**.
6. [ ] Targets: `GIS_SAF_SOA_Agent`. Click **Finish**.

## 5.2 Module: `SMIJMSModule`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `SMIJMSModule`. Click **Next**.
3. [ ] Targets: `osb_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `SMIJMSSubdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `SMIJMSSubdeployment`. Click **Next**.
6. [ ] Targets: `SMIJMSServer1`. Click **Finish**.

**Add subdeployment `SMIJMSSubdeployment1`:**
7. [ ] Open the module → **Subdeployments** tab → **New**.
8. [ ] Subdeployment Name: `SMIJMSSubdeployment1`. Click **Next**.
9. [ ] Targets: `SMIJMSServer1`. Click **Finish**.

**Add queue `CGI_DCCAlertQueue`:**
10. [ ] Open the module → **New** → select **Queue**. Click **Next**.
11. [ ] Name: `CGI_DCCAlertQueue`. JNDI Name: `jms/CGI_DCCAlertQueue`. Click **Next**.
12. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `LoggingQueue`:**
13. [ ] Open the module → **New** → select **Queue**. Click **Next**.
14. [ ] Name: `LoggingQueue`. JNDI Name: `jms/LoggingQueue`. Click **Next**.
15. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `MPRSSmartMeterQueue`:**
16. [ ] Open the module → **New** → select **Queue**. Click **Next**.
17. [ ] Name: `MPRSSmartMeterQueue`. JNDI Name: `jms/MPRSSmartMeterQueue`. Click **Next**.
18. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `OMSAlertQueue`:**
19. [ ] Open the module → **New** → select **Queue**. Click **Next**.
20. [ ] Name: `OMSAlertQueue`. JNDI Name: `jms/OMSAlertQueue`. Click **Next**.
21. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `OMSOutageIncidentQueue`:**
22. [ ] Open the module → **New** → select **Queue**. Click **Next**.
23. [ ] Name: `OMSOutageIncidentQueue`. JNDI Name: `jms/OMSOutageIncidentQueue`. Click **Next**.
24. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SIMSAlertQueue`:**
25. [ ] Open the module → **New** → select **Queue**. Click **Next**.
26. [ ] Name: `SIMSAlertQueue`. JNDI Name: `jms/SIMSAlertQueue`. Click **Next**.
27. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SLAAlertQueue`:**
28. [ ] Open the module → **New** → select **Queue**. Click **Next**.
29. [ ] Name: `SLAAlertQueue`. JNDI Name: `jms/SLAAlertQueue`. Click **Next**.
30. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SSEPD_ConsumeSmartMeterResFromCGI`:**
31. [ ] Open the module → **New** → select **Queue**. Click **Next**.
32. [ ] Name: `SSEPD_ConsumeSmartMeterResFromCGI`. JNDI Name: `jms/SSEPD_ConsumeSmartMeterResFromCGI`. Click **Next**.
33. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SSEPD_UpdateSmartMeterInCGI`:**
34. [ ] Open the module → **New** → select **Queue**. Click **Next**.
35. [ ] Name: `SSEPD_UpdateSmartMeterInCGI`. JNDI Name: `jms/SSEPD_UpdateSmartMeterInCGI`. Click **Next**.
36. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SSGSmartMeterQueue`:**
37. [ ] Open the module → **New** → select **Queue**. Click **Next**.
38. [ ] Name: `SSGSmartMeterQueue`. JNDI Name: `jms/SSGSmartMeterQueue`. Click **Next**.
39. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SubmitServiceRequestQueue`:**
40. [ ] Open the module → **New** → select **Queue**. Click **Next**.
41. [ ] Name: `SubmitServiceRequestQueue`. JNDI Name: `jms/SubmitServiceRequestQueue`. Click **Next**.
42. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add queue `SubmitServiceRequestResponseQueue`:**
43. [ ] Open the module → **New** → select **Queue**. Click **Next**.
44. [ ] Name: `SubmitServiceRequestResponseQueue`. JNDI Name: `jms/SubmitServiceRequestResponseQueue`. Click **Next**.
45. [ ] Subdeployment: `SMIJMSSubdeployment`. Click **Finish**.

**Add uniform distributed queue `OMSOutageCallbackQueue`:**
46. [ ] Open the module → **New** → select **Distributed Queue → Uniform**. Click **Next**.
47. [ ] Name: `OMSOutageCallbackQueue`. JNDI Name: `jms/OMSOutageCallbackQueue`. Click **Next**.
48. [ ] Subdeployment: `SMIJMSSubdeployment1`. Click **Finish**.
49. [ ] Open the new destination → set **Load Balancing Policy** to
       **Round-Robin**. Save.

**Add its Connection Factory `SMIJMSConnectionFactory` (default targeting -- inherits `osb_cluster`):**
50. [ ] Open the module → **New** → select **Connection Factory**. Click
       **Next**.
51. [ ] Name: `SMIJMSConnectionFactory`. JNDI Name: `jms/SMIJMSConnectionFactory`. Click **Next**.
52. [ ] On the Targeting page: leave **Default Targeting Enabled** checked.
       Do **not** select a subdeployment (this CF inherits `osb_cluster`). Click **Finish**.

---

## ➡ End of Phase 5

- [ ] Click **Activate Changes**.
- [ ] Verify each of the 2 modules lists its subdeployment(s) and its
      resources under **Services → Messaging → JMS Modules → [module
      name]**.
- [ ] Verify the 1 default-targeted connection factories
      (`SMIJMSConnectionFactory`) each show **Default
      Targeting** on their **Targets** tab, with no subdeployment selected.
- [ ] Verify `OMSOutageCallbackQueue`'s Load Balancing Policy shows **Round-Robin**.

---

# PHASE 6 — Adapter Deployments

Start a fresh **Lock & Edit** session before object 6.1. Every JMS module
from Phase 5 must exist before you deploy adapters whose connection
instances reference JMS resources created there.

⚠️ **Every connection instance listed below has host/user/password/JNDI
credentials baked into its plan XML that were not exported from the source
domain.** After each adapter is redeployed, you must open its Outbound
Connection Pools and manually review/set these for the target environment
— the steps below tell you which connection instances to check, not what
values to put in them (get those from the application owner / vault).

*(Source paths below use the source domain's Oracle home,
`/spddevfu/products/middleware/soa/soa`
(or a parent of it) — substitute your target Oracle
home if the `.rar`/plan files are staged at a different path on the target
host.)*

## 6.1 Adapter: `DbAdapter`

1. [ ] **Deployments → Install** (or select the existing `DbAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/spddevfu/products/middleware/soa/soa/connectors/DbAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/spddevfu/products/middleware/soa/soa/Plan_Smart_DB_Adaptor.xml`. Click **Finish**.
6. [ ] Open the deployed `DbAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for each of these 14 connection instances
       below:
   - [ ] `eis/DB/FaultDBTest`
   - [ ] `eis/DB/FaultDatabase`
   - [ ] `eis/DB/FaultDatabaseXA`
   - [ ] `eis/DB/MPRS_DMT_NORTH_CGI_XA`
   - [ ] `eis/DB/MPRS_DMT_SOUTH_CGI_XA`
   - [ ] `eis/DB/MPRS_NORTH_CGI_XA`
   - [ ] `eis/DB/MPRS_SOA_NORTH_XA`
   - [ ] `eis/DB/MPRS_SOA_SOUTH_XA`
   - [ ] `eis/DB/MPRS_SOUTH_CGI_XA`
   - [ ] `eis/DB/SIMSOMSIntegration`
   - [ ] `eis/DB/SIMSOMSIntegration_XA`
   - [ ] `eis/DB/SIMS_XA`
   - [ ] `eis/DB/SSE_XXCUST_XA`
   - [ ] `eis/DB/Test`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

## 6.2 Adapter: `JmsAdapter`

1. [ ] **Deployments → Install** (or select the existing `JmsAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/spddevfu/products/middleware/soa/soa/connectors/JmsAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/spddevfu/products/middleware/soa/soa/connectors/Plan_Smart_JMS_Adaptor.xml`. Click **Finish**.
6. [ ] Open the deployed `JmsAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for each of these 25 connection instances
       below:
   - [ ] `eis/activemq/Queue`
   - [ ] `eis/aqjms/EDNLocalTxDurableTopic`
   - [ ] `eis/aqjms/EDNLocalTxTopic`
   - [ ] `eis/aqjms/EDNxaDurableTopic`
   - [ ] `eis/aqjms/EDNxaTopic`
   - [ ] `eis/aqjms/Queue`
   - [ ] `eis/aqjms/Topic`
   - [ ] `eis/fioranomq/Topic`
   - [ ] `eis/jbossmq/Queue`
   - [ ] `eis/jms/SMICF`
   - [ ] `eis/jms/aiaB2BQueueCF`
   - [ ] `eis/jms/aiaErrorTopicCF`
   - [ ] `eis/pramati/Queue`
   - [ ] `eis/sunmq/Queue`
   - [ ] `eis/tibjms/Queue`
   - [ ] `eis/tibjms/Topic`
   - [ ] `eis/tibjmsDirect/Queue`
   - [ ] `eis/tibjmsDirect/Topic`
   - [ ] `eis/webspheremq/Queue`
   - [ ] `eis/wls/EDNLocalTxDurableTopic`
   - [ ] `eis/wls/EDNLocalTxTopic`
   - [ ] `eis/wls/EDNxaDurableTopic`
   - [ ] `eis/wls/EDNxaTopic`
   - [ ] `eis/wls/Queue`
   - [ ] `eis/wls/Topic`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

---

## ➡ End of Phase 6

- [ ] Click **Activate Changes**.
- [ ] Verify: **Deployments** shows `DbAdapter`, `JmsAdapter` all as **Active**.
- [ ] For each adapter, open **Testing** or **Monitoring** and confirm each
      connection instance you configured above resolves successfully.

---

# 8. Full Post-Release Verification

Go through every row below in order — do not skip any even if an earlier
phase's own checklist already covered part of it.

1. [ ] **Data Sources:** each of the 15 → Monitoring → Testing → Test.
       Success for all of them.
2. [ ] **Persistent Stores:** all 1 present in the list with correct
       targets.
3. [ ] **JMS Servers:** all 1 show Health = OK under Monitoring.
4. [ ] **JMS runtime bindings:** Environment → Servers → *each server* →
       **View JNDI Tree** — confirm every `jms/...` name from Phase 5
       appears bound.
5. [ ] **SAF Agents:** all 1 show up under Monitoring with the correct
       Logging setting.
6. [ ] **Connection Factory targeting:** `SMIJMSConnectionFactory` — each shows **Default
       Targeting** on its Targets tab (no subdeployment).
7. [ ] **Adapters:** all 2 show **Active** under Deployments.
8. [ ] **Adapter connection instances:** all 39 present under their
       respective Outbound Connection Pools and testing OK.

---

# 9. Rollback

- **Within a phase, before Activate Changes:** Change Center → **Undo All
  Changes**. This discards everything staged in the current edit session.
- **After Activate Changes:** delete the objects created in that phase, in
  reverse creation order, or restore the pre-change config backup taken in
  §0.
- Objects created by this plan are additive and don't touch existing
  config, so a partial rollback is low-risk — the only danger is if you
  target something new over an existing object with the same name, which
  none of these object names should collide with on a clean target domain.

---

# 10. Known Gaps — Confirm Before Go-Live

| # | Item | What to do |
|---|---|---|
| 1 | 15 JDBC passwords | None were exported — get from vault/DBA before Phase 1's post-steps. |
| 2 | Adapter connection-instance credentials | Baked into each plan XML, not exported — review/set all 39 instances in Phase 6 before go-live. |
| 3 | Adapter source/plan paths | Substitute your target Oracle home for the source paths throughout Phase 6. |
| 4 | Target infrastructure names | If your target domain's `AdminServer`, `osb_cluster`, `osb_server1`, `soa_cluster`, `soa_server1` differ, substitute your names everywhere those appear in this plan -- everything else (object names) must be created exactly as written. |

---

# 11. Extractor Warnings & Uncovered Objects

Read this section even when every Phase above is ticked off. It covers two kinds of
thing the extractor captured that the Phases above cannot fully turn into a
click-through procedure on their own:

1. **Referential-integrity warnings** -- the extractor found a reference (an error
   destination, a subdeployment) that didn't resolve to a real object on the source
   domain, and safely nulled it rather than exporting something broken. 
2. **SAF Imported Destinations** -- a real WebLogic object type this generator does
   not yet have a validated click-through procedure for.

No JNDI name collisions were found across the data sources in this export — every JNDI name is claimed by exactly one data source.

No referential-integrity warnings were reported by the extractor for this export -- every destination/template/error-handling reference captured here resolved to a real object on the source domain.

No SAF Imported Destinations were captured in this export.


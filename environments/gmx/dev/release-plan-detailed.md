# Release Plan (Detailed, Step-by-Step) — SOA Suite Domain JMS / Infrastructure Rebuild

**Domain / Environment:** `gmx` / `dev`
**Audience:** this version assumes no familiarity with this specific migration.
Every object is written out individually — there is no "repeat this procedure
for each row" step. Work top to bottom, tick each box, do not skip ahead.

**Source of values:** `export.json` (generated by `extract_objects.py`), domain
`gmx` / environment `dev`.
**Target:** WebLogic Admin Console (manual, click-through deployment).
**Companion file:** `release-plan.md` in this same folder is the condensed
version of this plan (procedures + lookup tables).
**Generated by:** `gorelease` (Go port of `extract/gen_release_plan.py`) — re-run against a fresh export if
the source domain's config changes; do not hand-edit this file, edit the export
or the generator instead.

---

## 0. Before You Start

- [ ] You have the Admin Console URL and an admin login for the **target**
      domain.
- [ ] You know whether the target domain's server/cluster names match the
      source domain's names exactly. The source names used throughout this
      plan are: `AdminServer`, `osb_cluster`, `osb_server1`, `soa_cluster`, `soa_server1`. **If your target uses different names, substitute your
      names everywhere this plan says one of those names — object names
      (data sources, stores, queues, etc.) are NOT renamed, only these
      infrastructure names.**
- [ ] You have the 6 database passwords for the data sources in Phase 1
      (from vault / DBA — none were exported from the source domain).
- [ ] You've confirmed `SOALocalTxDataSource` already exists on the target domain (referenced
      by persistent store `JMS_DB_Store` — this plan does not create it).
- [ ] The 4 adapter `.rar` files and 4 deployment plan `.xml` files (Phase 6)
      are staged somewhere the target Admin Server can read them.
- [ ] You've taken a config backup / snapshot of the target domain.
- [ ] **This plan is out of scope for:** SOA composites, OSB projects, and
      any system/internal WebLogic objects that come from the domain
      template or product install.

---

## 1. Starting an Edit Session

Do this once now, and again at the start of every Phase below (a Phase always
ends with **Activate Changes**, which closes the session).

1. [ ] Open a browser to the target Admin Console, e.g.
       `http://<admin-host>:<port>/console`.
2. [ ] Log in with the admin account.
3. [ ] Top-left corner: find the **Change Center** panel.
4. [ ] Click **Lock & Edit**. The console now shows you are in an edit
       session — everything you do below is staged, not live, until you
       activate it.

**At the end of each Phase** (marked with a ➡ in this document):

5. [ ] Click **Activate Changes** in the Change Center.
6. [ ] If the console reports errors for that phase, click **Undo All
       Changes**, fix the problem, and start that phase's steps again from
       Lock & Edit.
7. [ ] If it succeeds, move to the next Phase and start a fresh Lock & Edit.

Do not activate mid-phase — a partially-created phase can leave dangling
references.

---

## 2. Order of Work

Do the phases strictly in this order. Each later phase references objects
created in an earlier one.

1. Phase 1 — 6 JDBC Data Sources
2. Phase 2 — 9 Persistent Stores (6 File Stores + 3 JDBC Stores)
3. Phase 3 — 4 JMS Servers
4. Phase 4 — 4 SAF Agents
5. Phase 5 — 7 JMS Modules (with their subdeployments, queues, connection
   factories, and SAF error handlings)
6. Phase 6 — 4 Adapter Deployments

---

# PHASE 1 — JDBC Data Sources

Start a fresh **Lock & Edit** session (§1) before object 1.1 below.

For every data source in this phase you will do the same six-page wizard,
then a post-creation step to overwrite three fields the wizard doesn't set
correctly (URL, Driver Class Name, Properties) plus the password and pool
sizing. This is deliberate — the wizard's assembled values are not trusted;
what you type in the **Post-Step** for each object below is the value that
must end up saved.

---

## 1.1 Data Source: `CommonAuditLog`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `CommonAuditLog`
   - JNDI Name: `jdbc/CommonAuditLog`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `CBRMDEV`
   - Host Name: `porud455`
   - Port: `1526`
   - Database User Name: `MAXIMO`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": **tick nothing** — this data source is
       untargeted on the source domain. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@porud455:1526:CBRMDEV`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=MAXIMO`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.2 Data Source: `FaultDataSource`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `FaultDataSource`
   - JNDI Name: `jdbc/FaultIntegration_XA`
   - Database Type: **MS SQL Server**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `weblogic.jdbc.sqlserver.SQLServerDriver`
       — **Microsoft's MS SQL Server Driver (Type 4 XA)**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `Faults_DEV`
   - Host Name: `AZUWDEV000536`
   - Port: `1433`
   - Database User Name: `oms_fault`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:weblogic:sqlserver://AZUWDEV000536:1433;allowPortWithNamedInstance=true`
   - Driver Class Name: `weblogic.jdbc.sqlserver.SQLServerDriver`
   - Properties: `allowPortWithNamedInstance=true`, `databaseName=Faults_DEV`, `portNumber=1433`, `serverName=AZUWDEV000536`, `user=oms_fault`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL SELECT 1`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.3 Data Source: `SIMSOMSIntegration_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `SIMSOMSIntegration_XA`
   - JNDI Name: `jdbc/SIMSOMSIntegration_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `SIMSDEV`
   - Host Name: `havud301`
   - Port: `1526`
   - Database User Name: `sims2`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havud301:1526:SIMSDEV`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=sims2`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.4 Data Source: `WAM2JMSLocalDataSource`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `WAM2JMSLocalDataSource`
   - JNDI Name: `jdbc/WAM2JMSLocalDataSource`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.OracleDriver`
       — **Oracle's Driver (Thin) for Service connections (non-XA)**. Click **Next**.
5. [ ] Page "Transaction Options": **uncheck** "Supports Global Transactions" entirely -- this data source does not use a global transaction protocol. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `DBMRDV01`
   - Host Name: `havut049`
   - Port: `1526`
   - Database User Name: `GIS_XREF`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//havut049:1526/DBMRDV01`
   - Driver Class Name: `oracle.jdbc.OracleDriver`
   - Properties: `user=GIS_XREF`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `15`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.5 Data Source: `WAM_XXCUST_XA`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `WAM_XXCUST_XA`
   - JNDI Name: `jdbc/WAM_XXCUST_XA`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, but set the radio to **One-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `DBMRDV01`
   - Host Name: `havut049`
   - Port: `1526`
   - Database User Name: `WAM_XXCUST`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@havut049:1526:DBMRDV01`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=WAM_XXCUST`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `1`
   - Maximum Capacity: `3`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

## 1.6 Data Source: `XrefDataSource`

**Wizard:**
1. [ ] Domain Structure (left tree) → **Services → Data Sources**.
2. [ ] Click **New → Generic Data Source**.
3. [ ] Page "Create a New JDBC Data Source":
   - Name: `XrefDataSource`
   - JNDI Name: `jdbc/xref`
   - Database Type: **Oracle**
   - Click **Next**.
4. [ ] Page "Database Driver": choose the driver matching `oracle.jdbc.xa.client.OracleXADataSource`
       — **Oracle's Driver (Thin XA) for Service connections; Versionless and Version Specific Connections**. Click **Next**.
5. [ ] Page "Transaction Options": leave **Supports Global Transactions** checked, and leave the radio on **Two-Phase Commit**. Click **Next**.
6. [ ] Page "Connection Properties": enter (these only assemble a URL the
       wizard shows you next — you will overwrite it in the Post-Step):
   - Database Name: `DBMRDV01`
   - Host Name: `havut049`
   - Port: `1526`
   - Database User Name: `GIS_XREF`
   - Password / Confirm Password: leave blank for now (set in Post-Step).
   - Click **Next**.
7. [ ] Page "Test Database Connection": ignore the assembled URL shown
       here — you do not need to click Test Configuration (it will fail
       with no password). Click **Next**.
8. [ ] Page "Select Targets": tick `AdminServer`, `osb_cluster`, `soa_cluster`. Click **Finish**.

**Post-Step (open the data source you just created → Configuration tab):**
9. [ ] **Connection Pool** sub-tab — set:
   - URL: `jdbc:oracle:thin:@//havut049:1526/DBMRDV01`
   - Driver Class Name: `oracle.jdbc.xa.client.OracleXADataSource`
   - Properties: `user=GIS_XREF`
   - Password: ⚠️ **obtain from vault/DBA** — set here and in Confirm Password.
   - Initial Capacity: `5`
   - Maximum Capacity: `150`
   - Minimum Capacity: `1`
10. [ ] **Connection Pool → Advanced** (expand the Advanced link):
   - Test Table Name: `SQL ISVALID`
   - Test Connections On Reserve: **unchecked (No)**
11. [ ] Click **Save**.

---

## ➡ End of Phase 1

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Data Sources** lists all 6: `CommonAuditLog`, `FaultDataSource`, `SIMSOMSIntegration_XA`, `WAM2JMSLocalDataSource`, `WAM_XXCUST_XA`, `XrefDataSource`.
- [ ] For each of the 5 **targeted** ones (`FaultDataSource`, `SIMSOMSIntegration_XA`, `WAM2JMSLocalDataSource`, `WAM_XXCUST_XA`, `XrefDataSource`), open it → **Monitoring → Testing** tab → select a target server →
      click **Test Data Source**. Confirm success (this requires the
      password to be correct).
- [ ] `CommonAuditLog` untargeted, so it cannot be tested this way — just confirm it appears in the list with the right URL.

---

# PHASE 2 — Persistent Stores

Start a fresh **Lock & Edit** session before object 2.1.

There are two kinds: **File Stores** (data written to disk files) and
**JDBC Stores** (data written to a database table via a data source). Do
all 6 file stores first, then all 3 JDBC stores — order between them
doesn't matter, but both must finish before Phase 3.

---

## 2.1 File Store: `GIS_OSB_FileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `GIS_OSB_FileStore`. Target: `osb_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Set **Directory**: `GIS_OSB_FileStore`.
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.2 File Store: `GIS_SOA_FileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `GIS_SOA_FileStore`. Target: `soa_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.3 File Store: `MNFileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `MNFileStore`. Target: `osb_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.4 File Store: `MNSOARemoteFileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `MNSOARemoteFileStore`. Target: `soa_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.5 File Store: `WAM2_JMS_FILE_STORE`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `WAM2_JMS_FILE_STORE`. Target: `osb_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.6 File Store: `WAMFileStore`

1. [ ] **Services → Persistent Stores → New → Create FileStore**.
2. [ ] Name: `WAMFileStore`. Target: `osb_server1`. Click **OK**.
3. [ ] Open the store you just created.
4. [ ] Leave **Directory** at its default (blank).
5. [ ] Expand **Advanced** → set **Synchronous Write Policy** to **Direct-Write**.
6. [ ] Click **Save**.

## 2.7 JDBC Store: `JMS_DB_Store`

1. [ ] **Services → Persistent Stores → New → Create JDBCStore**.
2. [ ] Name: `JMS_DB_Store`.
3. [ ] Target: `AdminServer`.
4. [ ] Data Source: **`SOALocalTxDataSource`** ⚠️ — this must already exist on the target (referenced but not exported by this extract; confirm before proceeding).
5. [ ] Prefix Name: leave blank (none specified for this store).
6. [ ] Click **OK**, then **Save** if prompted.

## 2.8 JDBC Store: `WAM2JDBCStore_MS1`

1. [ ] **Services → Persistent Stores → New → Create JDBCStore**.
2. [ ] Name: `WAM2JDBCStore_MS1`.
3. [ ] Target: click the target picker and select the **migratable target**
       entry named **`osb_server1 (migratable)`** — not the plain server entry.
4. [ ] Data Source: **`WAM2JMSLocalDataSource`**
5. [ ] Click **OK**.
6. [ ] Open the store you just created → set **Prefix Name**: `WAM2Store_MS1`.
7. [ ] Click **Save**.

## 2.9 JDBC Store: `WAM2JDBCStore_MS2`

1. [ ] **Services → Persistent Stores → New → Create JDBCStore**.
2. [ ] Name: `WAM2JDBCStore_MS2`.
3. [ ] Target: click the target picker and select the **migratable target**
       entry named **`soa_server1 (migratable)`** — not the plain server entry.
4. [ ] Data Source: **`WAM2JMSLocalDataSource`**
5. [ ] Click **OK**.
6. [ ] Open the store you just created → set **Prefix Name**: `WAM2Store_MS2`.
7. [ ] Click **Save**.

---

## ➡ End of Phase 2

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Persistent Stores** lists all 9: the 6 file
      stores (`GIS_OSB_FileStore`, `GIS_SOA_FileStore`, `MNFileStore`, `MNSOARemoteFileStore`, `WAM2_JMS_FILE_STORE`, `WAMFileStore`) and the 3 JDBC stores (`JMS_DB_Store`, `WAM2JDBCStore_MS1`, `WAM2JDBCStore_MS2`), each with the target you set
      above.

---

# PHASE 3 — JMS Servers

Start a fresh **Lock & Edit** session before object 3.1. Each JMS server
below references a persistent store created in Phase 2 — Phase 2 must be
activated first.

## 3.1 JMS Server: `GIS_OSB_JMS_Server`

1. [ ] Domain Structure → **Services → Messaging → JMS Servers → New**.
2. [ ] Name: `GIS_OSB_JMS_Server`.
3. [ ] Persistent Store: `GIS_OSB_FileStore`. Click **Next**.
4. [ ] Target: `osb_server1`. Click **Finish**.
5. [ ] Leave Bytes Maximum / Messages Maximum at their unlimited defaults —
       no change needed.

## 3.2 JMS Server: `GIS_SOA_JMS_Server`

1. [ ] Domain Structure → **Services → Messaging → JMS Servers → New**.
2. [ ] Name: `GIS_SOA_JMS_Server`.
3. [ ] Persistent Store: `GIS_SOA_FileStore`. Click **Next**.
4. [ ] Target: `soa_server1`. Click **Finish**.
5. [ ] Leave Bytes Maximum / Messages Maximum at their unlimited defaults —
       no change needed.

## 3.3 JMS Server: `MNSOARemoteJMSServer`

1. [ ] Domain Structure → **Services → Messaging → JMS Servers → New**.
2. [ ] Name: `MNSOARemoteJMSServer`.
3. [ ] Persistent Store: `MNSOARemoteFileStore`. Click **Next**.
4. [ ] Target: `soa_server1`. Click **Finish**.
5. [ ] Leave Bytes Maximum / Messages Maximum at their unlimited defaults —
       no change needed.

## 3.4 JMS Server: `WAMJMSServer`

1. [ ] Domain Structure → **Services → Messaging → JMS Servers → New**.
2. [ ] Name: `WAMJMSServer`.
3. [ ] Persistent Store: `WAMFileStore`. Click **Next**.
4. [ ] Target: `osb_server1`. Click **Finish**.
5. [ ] Leave Bytes Maximum / Messages Maximum at their unlimited defaults —
       no change needed.

---

## ➡ End of Phase 3

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Messaging → JMS Servers** lists all 4: `GIS_OSB_JMS_Server`, `GIS_SOA_JMS_Server`, `MNSOARemoteJMSServer`, `WAMJMSServer` — each
      bound to the persistent store you set.
- [ ] Check **Monitoring** tab for each — Health should read OK once the
      target server has restarted (or immediately if this is a dynamic
      cluster that doesn't require a restart).

---

# PHASE 4 — SAF (Store-and-Forward) Agents

Start a fresh **Lock & Edit** session before object 4.1.

**Every agent below shares these common advanced settings — set them on
each agent after creating it, exactly as listed:**
- Retry Delay Base: `20000` ms
- Retry Delay Maximum: `180000` ms
- Retry Multiplier: `1`
- Acknowledge Interval: `-1` (this is the console default — leave it alone)
- Window Size: `10`
- Time-To-Live: `0` (unlimited)
- **Logging: ENABLED** ⚠️ this is not the console default (default is disabled) — every one of the agents below must have logging turned ON.

## 4.1 SAF Agent: `GIS_PublishChangeSet_SAF`

1. [ ] Domain Structure → **Services → Messaging → Store-and-Forward Agents
       → New**.
2. [ ] Name: `GIS_PublishChangeSet_SAF`.
3. [ ] Persistent Store: `GIS_OSB_FileStore`.
4. [ ] Agent Type: **Both** (this agent both sends and receives). Click **Next**.
5. [ ] Target: **tick nothing** — this agent is untargeted on the source
       domain. Click **Finish**.
6. [ ] Open the agent → set the common settings listed above, including
       **Logging = ENABLED**. Save.
7. [ ] ⚠️ Note: leaving this untargeted matches the source, but confirm
       with the application owner before go-live whether that's intended
       or an oversight on the source domain.

## 4.2 SAF Agent: `GIS_SAF_Agent`

1. [ ] Domain Structure → **Services → Messaging → Store-and-Forward Agents
       → New**.
2. [ ] Name: `GIS_SAF_Agent`.
3. [ ] Persistent Store: `GIS_OSB_FileStore`.
4. [ ] Agent Type: **Sending-only** (this agent only sends (forwards outbound messages)). Click **Next**.
5. [ ] Target: `osb_server1`. Click **Finish**.
6. [ ] Open the agent → set the common settings listed above, including
       **Logging = ENABLED**. Save.

## 4.3 SAF Agent: `GIS_SAF_SOA_Agent`

1. [ ] Domain Structure → **Services → Messaging → Store-and-Forward Agents
       → New**.
2. [ ] Name: `GIS_SAF_SOA_Agent`.
3. [ ] Persistent Store: `GIS_SOA_FileStore`.
4. [ ] Agent Type: **Sending-only** (this agent only sends (forwards outbound messages)). Click **Next**.
5. [ ] Target: `soa_server1`. Click **Finish**.
6. [ ] Open the agent → set the common settings listed above, including
       **Logging = ENABLED**. Save.

## 4.4 SAF Agent: `MNSAFAgent`

1. [ ] Domain Structure → **Services → Messaging → Store-and-Forward Agents
       → New**.
2. [ ] Name: `MNSAFAgent`.
3. [ ] Persistent Store: `MNFileStore`.
4. [ ] Agent Type: **Sending-only** (this agent only sends (forwards outbound messages)). Click **Next**.
5. [ ] Target: **tick nothing** — this agent is untargeted on the source
       domain. Click **Finish**.
6. [ ] Open the agent → set the common settings listed above, including
       **Logging = ENABLED**. Save.
7. [ ] ⚠️ Note: leaving this untargeted matches the source, but confirm
       with the application owner before go-live whether that's intended
       or an oversight on the source domain.

---

## ➡ End of Phase 4

- [ ] Click **Activate Changes**.
- [ ] Verify: **Services → Messaging → Store-and-Forward Agents** lists all 4:
      `GIS_PublishChangeSet_SAF`, `GIS_SAF_Agent`, `GIS_SAF_SOA_Agent`, `MNSAFAgent`.
- [ ] Open each and confirm Logging matches the value noted above — this is
      easy to miss since it isn't always the console default.

---

# PHASE 5 — JMS Modules

Start a fresh **Lock & Edit** session before object 5.1. Each module below
is done completely (module → subdeployment(s) → destinations/CFs/error
handlings) before moving to the next module.

**General pattern for every module in this phase — the exact wizard steps
you'll repeat, spelled out once here for reference. Each module section
below tells you exactly what values to type at each of these steps; you
don't need to look anything up elsewhere.**

- *Create the module itself:* **Services → Messaging → JMS Modules → New**
  → type Name → Next → tick Targets → Next → Finish.
- *Add a subdeployment* (do this before adding any destination that uses
  it): open the module → **Subdeployments** tab → New → type Subdeployment
  Name → Next → tick the Targets (a JMS server, a SAF agent, or a cluster,
  depending on the module) → Finish.
- *Add a Queue / Topic:* open the module → New → select the destination
  type → Next → type Name and JNDI Name → Next → select the Subdeployment
  → Finish.
- *Add a Distributed Queue/Topic (Uniform or plain):* same as above but
  select the distributed variant → after Finish, open it and set Load
  Balancing Policy.
- *Add a Connection Factory (default-targeted):* open the module → New →
  **Connection Factory** → Next → type Name and JNDI Name → Next → on the
  Targeting page, **leave "Default Targeting Enabled" checked and do not
  select a Subdeployment** → Finish.
- *Add a SAF Error Handling:* open the module → New → **SAF Error Handling**
  → Next → type Name → select Policy → Finish.

---

## 5.1 Module: `GIS_OSB_JMS_Module`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `GIS_OSB_JMS_Module`. Click **Next**.
3. [ ] Targets: `osb_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `GIS_OSB_JMS_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `GIS_OSB_JMS_Subdeployment`. Click **Next**.
6. [ ] Targets: `GIS_OSB_JMS_Server`. Click **Finish**.

**Add queue `GIS_FileNotificationJMSQueue`:**
7. [ ] Open the module → **New** → select **Queue**. Click **Next**.
8. [ ] Name: `GIS_FileNotificationJMSQueue`. JNDI Name: `jms/GIS_FileNotificationJMSQueue`. Click **Next**.
9. [ ] Subdeployment: `GIS_OSB_JMS_Subdeployment`. Click **Finish**.

**Add queue `TempQueue`:**
10. [ ] Open the module → **New** → select **Queue**. Click **Next**.
11. [ ] Name: `TempQueue`. JNDI Name: `jms/TempQueue`. Click **Next**.
12. [ ] Subdeployment: `GIS_OSB_JMS_Subdeployment`. Click **Finish**.

## 5.2 Module: `GIS_SAF_JMS_Module`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `GIS_SAF_JMS_Module`. Click **Next**.
3. [ ] Targets: `osb_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `GIS_SAF_JMS_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `GIS_SAF_JMS_Subdeployment`. Click **Next**.
6. [ ] Targets: `osb_cluster`. Click **Finish**.

**Add its SAF Error Handling `GIS_ErrorHandlerSAF_SOA`:**
7. [ ] Open the module → **New** → select **SAF Error Handling**. Click
       **Next**.
8. [ ] Name: `GIS_ErrorHandlerSAF_SOA`.
9. [ ] Policy: **Discard**. Click **Finish**.

## 5.3 Module: `GIS_SAF_SOA_JMS_Module`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `GIS_SAF_SOA_JMS_Module`. Click **Next**.
3. [ ] Targets: `soa_server1`. Click **Next**, then **Finish**.

**Add subdeployment `GIS_SAF_SOA_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `GIS_SAF_SOA_Subdeployment`. Click **Next**.
6. [ ] Targets: `GIS_SAF_SOA_Agent`. Click **Finish**.

## 5.4 Module: `GIS_SOA_JMS_Module`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `GIS_SOA_JMS_Module`. Click **Next**.
3. [ ] Targets: `soa_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `GIS_SOA_JMS_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `GIS_SOA_JMS_Subdeployment`. Click **Next**.
6. [ ] Targets: `GIS_SOA_JMS_Server`. Click **Finish**.

**Add queue `GIS_PublishChangeSetJMSQueue_SOA`:**
7. [ ] Open the module → **New** → select **Queue**. Click **Next**.
8. [ ] Name: `GIS_PublishChangeSetJMSQueue_SOA`. JNDI Name: `jms/GIS_PublishChangeSetJMSQueue_SOA`. Click **Next**.
9. [ ] Subdeployment: `GIS_SOA_JMS_Subdeployment`. Click **Finish**.

**Add queue `GIS_UpdateJobStatusJMSQueue_SOA`:**
10. [ ] Open the module → **New** → select **Queue**. Click **Next**.
11. [ ] Name: `GIS_UpdateJobStatusJMSQueue_SOA`. JNDI Name: `jms/GIS_UpdateJobStatusJMSQueue_SOA`. Click **Next**.
12. [ ] Subdeployment: `GIS_SOA_JMS_Subdeployment`. Click **Finish**.

## 5.5 Module: `MNSAFJMSModule`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `MNSAFJMSModule`. Click **Next**.
3. [ ] Targets: `osb_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `MNSAFSubDeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `MNSAFSubDeployment`. Click **Next**.
6. [ ] Targets: `MNSAFAgent`. Click **Finish**.

**Add its Connection Factory `MNSAFConnectionFactory` (default targeting -- inherits `osb_cluster`):**
7. [ ] Open the module → **New** → select **Connection Factory**. Click
       **Next**.
8. [ ] Name: `MNSAFConnectionFactory`. JNDI Name: `jms/MNSAFConnectionFactory`. Click **Next**.
9. [ ] On the Targeting page: leave **Default Targeting Enabled** checked.
       Do **not** select a subdeployment (this CF inherits `osb_cluster`). Click **Finish**.

**Add its SAF Error Handling `MNSAFErrorHandling`:**
10. [ ] Open the module → **New** → select **SAF Error Handling**. Click
       **Next**.
11. [ ] Name: `MNSAFErrorHandling`.
12. [ ] Policy: **Log**. Click **Finish**.

## 5.6 Module: `MNSOARemoteJMSModule`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `MNSOARemoteJMSModule`. Click **Next**.
3. [ ] Targets: `soa_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `MNSOARemoteSubDeploymentJmsModule`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `MNSOARemoteSubDeploymentJmsModule`. Click **Next**.
6. [ ] Targets: `MNSOARemoteJMSServer`. Click **Finish**.

**Add uniform distributed queue `MNSOARemoteQueue`:**
7. [ ] Open the module → **New** → select **Distributed Queue → Uniform**. Click **Next**.
8. [ ] Name: `MNSOARemoteQueue`. JNDI Name: `jms/MNSOARemoteQueue`. Click **Next**.
9. [ ] Subdeployment: `MNSOARemoteSubDeploymentJmsModule`. Click **Finish**.
10. [ ] Open the new destination → set **Load Balancing Policy** to
       **Round-Robin**. Save.

**Add its Connection Factory `MNSOARemoteCF` (default targeting -- inherits `soa_cluster`):**
11. [ ] Open the module → **New** → select **Connection Factory**. Click
       **Next**.
12. [ ] Name: `MNSOARemoteCF`. JNDI Name: `jms/MNSOARemoteCF`. Click **Next**.
13. [ ] On the Targeting page: leave **Default Targeting Enabled** checked.
       Do **not** select a subdeployment (this CF inherits `soa_cluster`). Click **Finish**.

## 5.7 Module: `WAMJMSModule`

**Create the module:**
1. [ ] **Services → Messaging → JMS Modules → New**.
2. [ ] Name: `WAMJMSModule`. Click **Next**.
3. [ ] Targets: `osb_cluster`. Click **Next**, then **Finish**.

**Add subdeployment `WAM_JMS_Subdeployment`:**
4. [ ] Open the module → **Subdeployments** tab → **New**.
5. [ ] Subdeployment Name: `WAM_JMS_Subdeployment`. Click **Next**.
6. [ ] Targets: `WAMJMSServer`. Click **Finish**.

**Add queue `WAM_CBRM_JMS_Q`:**
7. [ ] Open the module → **New** → select **Queue**. Click **Next**.
8. [ ] Name: `WAM_CBRM_JMS_Q`. JNDI Name: `jms/WAM_CBRM_JMS_Q`. Click **Next**.
9. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_Chime_JMS_Q`:**
10. [ ] Open the module → **New** → select **Queue**. Click **Next**.
11. [ ] Name: `WAM_Chime_JMS_Q`. JNDI Name: `jms/WAM_Chime_JMS_Q`. Click **Next**.
12. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_EH_JMS_Q`:**
13. [ ] Open the module → **New** → select **Queue**. Click **Next**.
14. [ ] Name: `WAM_EH_JMS_Q`. JNDI Name: `jms/WAM_EH_JMS_Q`. Click **Next**.
15. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_HICI_JMS_Q`:**
16. [ ] Open the module → **New** → select **Queue**. Click **Next**.
17. [ ] Name: `WAM_HICI_JMS_Q`. JNDI Name: `jms/WAM_HICI_JMS_Q`. Click **Next**.
18. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_IPS_JMS_Q`:**
19. [ ] Open the module → **New** → select **Queue**. Click **Next**.
20. [ ] Name: `WAM_IPS_JMS_Q`. JNDI Name: `jms/WAM_IPS_JMS_Q`. Click **Next**.
21. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_MX_JMS_Q`:**
22. [ ] Open the module → **New** → select **Queue**. Click **Next**.
23. [ ] Name: `WAM_MX_JMS_Q`. JNDI Name: `jms/WAM_MX_JMS_Q`. Click **Next**.
24. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add queue `WAM_SerialNum_JMS_Q`:**
25. [ ] Open the module → **New** → select **Queue**. Click **Next**.
26. [ ] Name: `WAM_SerialNum_JMS_Q`. JNDI Name: `jms/WAM_SerialNum_JMS_Q`. Click **Next**.
27. [ ] Subdeployment: `WAM_JMS_Subdeployment`. Click **Finish**.

**Add its Connection Factory `WAMJMSConnectionFactory` (default targeting -- inherits `osb_cluster`):**
28. [ ] Open the module → **New** → select **Connection Factory**. Click
       **Next**.
29. [ ] Name: `WAMJMSConnectionFactory`. JNDI Name: `jms/WAM_JMS_CF`. Click **Next**.
30. [ ] On the Targeting page: leave **Default Targeting Enabled** checked.
       Do **not** select a subdeployment (this CF inherits `osb_cluster`). Click **Finish**.

---

## ➡ End of Phase 5

- [ ] Click **Activate Changes**.
- [ ] Verify each of the 7 modules lists its subdeployment(s) and its
      resources under **Services → Messaging → JMS Modules → [module
      name]**.
- [ ] Verify the 3 default-targeted connection factories
      (`MNSAFConnectionFactory`, `MNSOARemoteCF`, `WAMJMSConnectionFactory`) each show **Default
      Targeting** on their **Targets** tab, with no subdeployment selected.
- [ ] Verify `MNSOARemoteQueue`'s Load Balancing Policy shows **Round-Robin**.

---

# PHASE 6 — Adapter Deployments

Start a fresh **Lock & Edit** session before object 6.1. Every JMS module
from Phase 5 must exist before you deploy adapters whose connection
instances reference JMS resources created there.

⚠️ **Every connection instance listed below has host/user/password/JNDI
credentials baked into its plan XML that were not exported from the source
domain.** After each adapter is redeployed, you must open its Outbound
Connection Pools and manually review/set these for the target environment
— the steps below tell you which connection instances to check, not what
values to put in them (get those from the application owner / vault).

*(Source paths below use the source domain's Oracle home,
`/gmxdevfu/products/middleware/soa/soa`
(or a parent of it) — substitute your target Oracle
home if the `.rar`/plan files are staged at a different path on the target
host.)*

## 6.1 Adapter: `DbAdapter`

1. [ ] **Deployments → Install** (or select the existing `DbAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/gmxdevfu/products/middleware/soa/soa/connectors/DbAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/gmxdevfu/products/middleware/soa/soa/XrefPlan.xml`. Click **Finish**.
6. [ ] Open the deployed `DbAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for each of these 4 connection instances
       below:
   - [ ] `eis/DB/SIMSOMSIntegration_XA`
   - [ ] `eis/DB/WAM_XXCUST`
   - [ ] `eis/DB/WAM_XXCUST_XA`
   - [ ] `eis/DB/XREF`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

## 6.2 Adapter: `JmsAdapter`

1. [ ] **Deployments → Install** (or select the existing `JmsAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/gmxdevfu/products/middleware/soa/soa/connectors/JmsAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/gmxdevfu/products/middleware/soa/soa/Plan.xml`. Click **Finish**.
6. [ ] Open the deployed `JmsAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for each of these 4 connection instances
       below:
   - [ ] `eis/jms/MNSAFConnectionFactory`
   - [ ] `eis/jms/MNSOARemoteCF`
   - [ ] `eis/jms/WAM_Chime_JMS_Q`
   - [ ] `eis/jms/WAM_EH_JMS_Q`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

## 6.3 Adapter: `AqAdapter`

1. [ ] **Deployments → Install** (or select the existing `AqAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/gmxdevfu/products/middleware/soa/soa/connectors/AqAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/gmxdevfu/products/middleware/soa/soa/AQPlan.xml`. Click **Finish**.
6. [ ] Open the deployed `AqAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for this connection instance
       below:
   - [ ] `eis/AQ/CommonAuditLog`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

## 6.4 Adapter: `FtpAdapter`

1. [ ] **Deployments → Install** (or select the existing `FtpAdapter` →
       **Update**, if one already exists from the domain template and you
       are replacing its plan).
2. [ ] Browse to the source `.rar`: `/gmxdevfu/products/middleware/soa/soa/connectors/FtpAdapter.rar`. Click **Next**.
3. [ ] Choose **Install this deployment as an application** (or, if
       updating, proceed with the update flow). Click **Next**.
4. [ ] Targets: tick `soa_cluster`, `osb_cluster`, `AdminServer`. Click **Next**.
5. [ ] Deployment Plan path: `/gmxdevfu/products/middleware/soa/soa/EO_Plan.xml`. Click **Finish**.
6. [ ] Open the deployed `FtpAdapter` → **Configuration → Outbound
       Connection Pools**. Review/set host, port, user, password for this connection instance
       below:
   - [ ] `eis/Ftp/EO_FtpAdapter`
7. [ ] Save each change, and click **Update Plan** if the console prompts
       you to persist the edit into the plan XML.

---

## ➡ End of Phase 6

- [ ] Click **Activate Changes**.
- [ ] Verify: **Deployments** shows `DbAdapter`, `JmsAdapter`, `AqAdapter`, `FtpAdapter` all as **Active**.
- [ ] For each adapter, open **Testing** or **Monitoring** and confirm each
      connection instance you configured above resolves successfully.

---

# 8. Full Post-Release Verification

Go through every row below in order — do not skip any even if an earlier
phase's own checklist already covered part of it.

1. [ ] **Data Sources:** each of the 6 → Monitoring → Testing → Test.
       Success for the 5 targeted ones; `CommonAuditLog` just needs to be
       present (untargeted, can't be tested this way).
2. [ ] **Persistent Stores:** all 9 present in the list with correct
       targets.
3. [ ] **JMS Servers:** all 4 show Health = OK under Monitoring.
4. [ ] **JMS runtime bindings:** Environment → Servers → *each server* →
       **View JNDI Tree** — confirm every `jms/...` name from Phase 5
       appears bound.
5. [ ] **SAF Agents:** all 4 show up under Monitoring with the correct
       Logging setting.
6. [ ] **Connection Factory targeting:** `MNSAFConnectionFactory`, `MNSOARemoteCF`, `WAMJMSConnectionFactory` — each shows **Default
       Targeting** on its Targets tab (no subdeployment).
7. [ ] **Adapters:** all 4 show **Active** under Deployments.
8. [ ] **Adapter connection instances:** all 10 present under their
       respective Outbound Connection Pools and testing OK.

---

# 9. Rollback

- **Within a phase, before Activate Changes:** Change Center → **Undo All
  Changes**. This discards everything staged in the current edit session.
- **After Activate Changes:** delete the objects created in that phase, in
  reverse creation order, or restore the pre-change config backup taken in
  §0.
- Objects created by this plan are additive and don't touch existing
  config, so a partial rollback is low-risk — the only danger is if you
  target something new over an existing object with the same name, which
  none of these object names should collide with on a clean target domain.

---

# 10. Known Gaps — Confirm Before Go-Live

| # | Item | What to do |
|---|---|---|
| 1 | 6 JDBC passwords | None were exported — get from vault/DBA before Phase 1's post-steps. |
| 2 | `SOALocalTxDataSource` | Must already exist on target — referenced by persistent store `JMS_DB_Store` (Phase 2). |
| 3 | Adapter connection-instance credentials | Baked into each plan XML, not exported — review/set all 10 instances in Phase 6 before go-live. |
| 4 | Adapter source/plan paths | Substitute your target Oracle home for the source paths throughout Phase 6. |
| 5 | Untargeted SAF agents | `GIS_PublishChangeSet_SAF`, `MNSAFAgent` — confirm with the app owner this is intentional, not an oversight, before matching it here. |
| 6 | Target infrastructure names | If your target domain's `AdminServer`, `osb_cluster`, `osb_server1`, `soa_cluster`, `soa_server1` differ, substitute your names everywhere those appear in this plan -- everything else (object names) must be created exactly as written. |

---

# 11. Extractor Warnings & Uncovered Objects

Read this section even when every Phase above is ticked off. It covers two kinds of
thing the extractor captured that the Phases above cannot fully turn into a
click-through procedure on their own:

1. **Referential-integrity warnings** -- the extractor found a reference (an error
   destination, a subdeployment) that didn't resolve to a real object on the source
   domain, and safely nulled it rather than exporting something broken. 
2. **SAF Imported Destinations** -- a real WebLogic object type this generator does
   not yet have a validated click-through procedure for.

No JNDI name collisions were found across the data sources in this export — every JNDI name is claimed by exactly one data source.

No referential-integrity warnings were reported by the extractor for this export -- every destination/template/error-handling reference captured here resolved to a real object on the source domain.

No SAF Imported Destinations were captured in this export.

